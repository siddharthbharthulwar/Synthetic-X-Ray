function [returnedMatrix] = rotateMatrix(inputMatrix, size)

%output matrix will be same size as input matrix

tempMatrix = zeros(size(inputMatrix));

for index i = 1:size
    
    slice = squeeze(inputMatrix(index,:,:));
    slice = r

end
